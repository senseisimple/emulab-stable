#!/usr/bin/perl

#!/bin/bash

#echo `pwd`
#exec `perl -mevent -I. -I/usr/testbed/lib bgmon.pl`

#use Cwd;
#print "cwd = ".getcwd."\n";

#print "Argc = $ARGC\n";
if( @ARGV < 1 ){
    die "usage: <path_to_bgmon.pl>";
}


#system "sudo perl -mevent -I. -I/usr/testbed/lib -d $ARGV[0]bgmon.pl &";
system "perl $ARGV[0]bgmon.pl -d $ARGV[0] &";
system "sudo rm iperfd.out iperfd.err";
system "sudo $ARGV[0]iperfd -s -p 5002 >& $ARGV[0]iperfd.err > $ARGV[0]iperfd.out &";

